# Kobe: Kotlin + Jersey HTTP Server

Kobe is a personal project to set up kotlin with JAX-RS annotations using Jersey.

Build with gradle:
```
gradle fatJar
```
Start the server:
```
java -jar build/libs/kobe-all.jar 
```
